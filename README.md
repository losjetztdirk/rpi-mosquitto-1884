# rpi-mosquitto

Docker image for Eclipse Mosquitto server made for Raspberry Pi.

## Installation

- Checkout from Github
- Add your certificates to `config/certs` (see https://dzone.com/articles/mqtt-security-securing-a-mosquitto-server)
- Using Docker-compose : `docker-compose up`